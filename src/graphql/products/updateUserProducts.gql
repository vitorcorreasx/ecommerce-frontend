mutation UpdateUserProducts($userId: ID!, $input: [ProductCart]!) {
  updateUserProducts(userId: $userId, input: $input) {
    products {
      id
      amount
      price
      title
      total
    }
  }
}