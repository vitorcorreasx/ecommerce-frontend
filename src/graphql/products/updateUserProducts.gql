mutation UpdateUserProducts($userStore: ID!, $input: [ProductCart]!) {
  updateUserProducts(userStore: $userStore, input: $input) {
    products {
      id
      amount
      price
      title
      total
    }
  }
}